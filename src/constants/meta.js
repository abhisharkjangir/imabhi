import ENV from './config';
import ASSET from './assets';

export default {
  default : {
    name : 'imabhi',
    title : 'imabhi',
    description : '',
    url : ENV.URL,
    img : ASSET.LOGO
  }
}
